# Tasks

## 1. Приоритет и потолок

- [x] 1.1 `priority_weight` 900 и `execution_timeout` в `pg_activity`, `log_events`,
      `test_connections`, `show_connections`; проверить разбор файлов на стенде
- [x] 1.2 Потолки выбраны с запасом над собственными таймаутами проверок (у проверки
      подключений это 15 секунд на соединение) — свериться по коду
- [x] 1.3 Регрессионные прогоны (`test_dags`, `test_hrp_operators`) не трогаем — проверить,
      что их настройки не изменились

## 2. Проверка и документация

- [x] 2.1 Прогон короткой проверки на стенде: приоритет виден в задаче, потолок не мешает
- [x] 2.2 `check/readme.md` и строки версий; `ruff check .`, `openspec validate --all --strict`
