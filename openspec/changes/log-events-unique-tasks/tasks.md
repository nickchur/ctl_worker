# Tasks

## 1. Отчёт считает задачи

- [ ] 1.1 `check/log_events.py`: в `SQL_BY_EVENT` и `SQL_BY_TASK` добавить счётчик
      различных попыток — `count(DISTINCT (dag_id, task_id, run_id, map_index,
      try_number))` в первом и `count(DISTINCT (run_id, map_index, try_number))` во
      втором, где группировка по дагу и задаче уже есть. Проверка: прогон на стенде
      возвращает у обоих запросов число задач не больше числа событий и не ноль при
      непустой выборке.
- [ ] 1.2 Положить в снимок `tasks` рядом с `total` и считать `share_pct` по задачам.
      Проверка: на данных, где одна задача дала тысячи событий, доля перестаёт превышать
      100%.
- [ ] 1.3 Порог `alert_after` сравнивать с числом задач; поправить его описание в форме и
      таблицу параметров в docstring. Проверка: прогон с порогом между числом задач и
      числом событий остаётся зелёным.
- [ ] 1.4 В сводке показывать оба числа так, чтобы разница читалась — «сбоев доставки N на
      M задачах». Проверка: заметка на прогоне со стенда содержит оба числа и долю.

## 2. Время в снимке

- [ ] 2.1 Привести `last_seen` и границу суток к МСК в тексте запросов через
      `AT TIME ZONE INTERVAL '+03:00'`; условие окна не трогать. Проверка: `last_seen`
      совпадает с временем события в Event Log интерфейса Airflow.
- [ ] 2.2 Печатать `ts` в МСК, оставив арифметику окна в UTC. Проверка: в снимке `ts` не
      меньше всех `last_seen` и отличается от них на разумные минуты, а не на три часа.

## 3. Проверка и выкладка

- [ ] 3.1 Замерить цену запросов с `DISTINCT` на журнале стенда за сутки и за неделю;
      если она заметно выросла — вписать числа в комментарий к потолкам окна рядом с
      прежними замерами. Проверка: замер выполнен, комментарий соответствует факту.
- [ ] 3.2 Перед выкладкой посмотреть Variable `tools_log_events_params`: сохранённого
      `alert_after` быть не должно. Проверка: ключа нет либо он обнулён.
- [ ] 3.3 `ruff check .`, `openspec validate --all --strict`,
      `python3 .claude/scripts/check_context.py`. Проверка: все три чисты.
- [ ] 3.4 Прогон `tools_log_events` на контуре с боевым журналом: сравнить сводку с
      предыдущей и убедиться, что число событий не изменилось, а доля стала осмысленной.
      Проверка: обе сводки приложены к PR.
