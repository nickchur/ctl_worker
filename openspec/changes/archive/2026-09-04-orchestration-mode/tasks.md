# Tasks

## 1. Режим в конфигурации

- [x] 1.1 Ключи `orchestrator` (`ctl`/`mixed`/`af`) и `pause_new_dags` в `ctl_config.py`:
      умолчания, списки значений в форме; проверить, что без ключей поведение прежнее
- [x] 1.2 Чтение режима в фабрике одним местом; проверить разбор под каждым значением

## 2. Фабрика

- [x] 2.1 `get_schedule` учитывает режим: `ctl` — только датасет ТФС, `mixed` — как сейчас,
      `af` — расписание всегда; проверить на стенде по одному воркфлоу каждого вида
- [x] 2.2 `is_paused_upon_creation` берётся из `pause_new_dags`; проверить на новом воркфлоу
- [x] 2.3 Предупреждение о двойном запуске в режиме `af` — по кэшу `ctl_workflows`, без
      обращения к API при разборе; проверить, что разбор не падает
- [x] 2.4 Галка `schedule_wf` в форме воркфлоу не действует в режиме `af`: ставить и снимать
      расписание в CTL из Airflow — значит своими руками сделать двойной запуск
      *Проверка:* в `af` галка не меняет `scheduled` в CTL, причина видна в заметке
- [x] 2.5 Приоритет ключей `is_paused`/`un_paused` над `pause_new_dags` подтверждён
      *Проверка:* `set_pause` зовётся в начале `run_prm`, то есть после создания дага

## 3. Проверка и документация

- [x] 3.1 Переключение режимов на стенде: `parse_time` и `serialized_gap` после каждого
- [x] 3.1a Архивные и симуляторные воркфлоу режимом не затронуты
      *Проверка:* `get_schedule` для архива возвращает расписание до ветвления по режиму;
      `ctl_test.py` прогнан под каждым режимом
- [ ] 3.1b Нагрузка переключения на alpha: ~677 записей `serialized_dag` за цикл,
      `serialized_gap` не держится выше пяти минут
      *Проверка:* переключать на подмножестве не выйдет — режим общий для профиля, поэтому
      смотреть надо окно затишья и метрики сразу после
- [x] 3.2 Ручной запуск из CTL проходит в каждом режиме (эмулятор)
- [x] 3.3 `ctl_worker/readme.md`, `openspec/project.md`, строки версий; `ruff`, `openspec validate`
