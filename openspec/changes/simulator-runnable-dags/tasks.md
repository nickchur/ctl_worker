# Tasks

## 1. Отбор запускаемых дагов

- [x] 1.1 Отбор по метабазе двумя запросами пакетом на весь список: `is_paused=false`, `is_active=true`, наличие в
      `serialized_dag`; проверить на стенде, что запаузенные не попадают
- [x] 1.2 Пустой отбор — пропуск с заметкой; число отсеянных в заметке
- [x] 1.3 Прогон режима `trigger` на стенде: раны создаются только у рабочих дагов

## 2. Документация

- [x] 2.1 `ctl_worker/readme.md` и строка версии; `ruff check .`, `openspec validate --all --strict`
