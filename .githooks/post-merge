#!/usr/bin/env bash
# Ветка обновилась (pull, merge) — подтянуть агентский контекст и показать расхождения.
# Хук не блокирует ничего: он сообщает, а не запрещает.
exec "$(git rev-parse --show-toplevel)/.githooks/_sync" "после merge"
