# Инструменты тестирования и проверки
*2026-08-31 23:20 MSK · v1.3 · Nick Churkin · [NSChurkin@sber.ru](mailto:NSChurkin@sber.ru)*

Данная директория содержит инструменты и DAG'и для верификации работоспособности кастомных операторов и системных компонентов платформы.

## Состав

### [show_connections.py](show_connections.py)
**Аудит и группировка Airflow Connections.**

Выводит список всех подключений, сгруппированных по типу. Используется для быстрой проверки конфигурации secret backend.
*   **Кэширование**: Результаты сохраняются в Airflow Variable `local_connections`.
*   **Интеграция**: Служит источником данных для DAG'а `test_connections`.

### [test_connections.py](test_connections.py)
**Мониторинг и проверка всех Airflow Connections.**

DAG автоматически обнаруживает все подключения и проверяет их доступность.
*   **Оптимизация**: Использует Airflow Variable `local_connections` в качестве источника данных.
*   **Стандартизация**: Статус пропущенных проверок помечается символом `☮️`.
*   **Отчетность**: Формирует сводную таблицу ✅/❌/☮️ в заметках DAG'а.

### [test_hrp_operators.py](test_hrp_operators.py)
**Исчерпывающий тестовый стенд для кастомных операторов (HRP Operators).**

Этот DAG предназначен для функционального и регрессионного тестирования операторов из пакета `hrp_operators`. Используется как обязательный этап валидации перед релизами и обновлением зависимостей Airflow.

#### Особенности реализации:
*   **Data Integrity**: Покрытие сложных типов данных, включая **Array (массивы)** в Postgres и ClickHouse.
*   **Quality Gate**: Тестовые таблицы создаются с полным описанием (COMMENT ON) для соответствия корпоративным стандартам качества.
*   **Валидация**: После каждой переливки выполняется автоматическая сверка количества строк и содержимого (включая проверку элементов массивов).
*   **Corner Cases**: Проверка обработки спецсимволов (`\n`, `\t`, `"`, `,`, `;`), NULL-значений и различных режимов сжатия (GZIP).
*   **Изоляция**: Полный цикл `Setup` -> `Test` -> `Cleanup` (удаление таблиц и файлов S3 даже при падении тестов).

#### Основные группы тестов:
1.  **pg_to_s3**: Выгрузка из Postgres в S3 с использованием `xstream_sanitize`.
2.  **s3_to_ch**: Загрузка из S3 в ClickHouse с валидацией массивов.
3.  **ch_to_s3**: Выгрузка из ClickHouse (Table, Query, Native).
4.  **db_to_db**: Переливки PG <-> CH, PG -> PG, CH -> PG.
5.  **s3_utils**: Проверка архивации и сверка хэш-сумм файлов.

### [db_cleanup.py](db_cleanup.py)
**Очистка метадаты Airflow (`tools_db_cleanup`).**

Удаляет устаревшие записи из метабазы прямыми SQL-запросами, без CTAS-архивирования.
*   **Параметры**: `retention_days` (180 по умолчанию, минимум 30), `dry_run`, `vacuum`, `reindex`, `custom`.
*   **Порционность**: таблицы больше 50 000 строк чистятся по диапазону дат.
*   **Отчётность**: заметка обновляется после каждой таблицы; финальный отчёт по размерам схемы `main` с delta к предыдущему запуску.
*   **Внимание**: `dry_run=False` по умолчанию — это реальное удаление.

### [log_cleanup.py](log_cleanup.py)
**Обслуживание бакета логов задач (`tools_log_cleanup`).**

Ежедневно (`17 5 * * *`) чистит логи задач. Бакет, префикс и подключение берутся из
`[logging] remote_base_log_folder` и `remote_log_conn_id`, а не прописаны именем.
*   **Параметры**: `months` (1 по умолчанию) и `days` (0) — возраст объектов для удаления.
*   **Таски**: `create_bucket` → `clean_logs` → `show_bucket_size`; каждая пишет заметку.
*   **Отчётность**: итоговый объём префикса и число объектов после очистки.

### [pg_activity.py](pg_activity.py)
**Сторож метабазы: зависшие сессии, долгие запросы, блокировки (`tools_pg_activity`).**

Каждые 10 минут снимает `pg_stat_activity` метабазы и разбирает находки на `idle in transaction`,
долгие активные запросы и блокировки (`pg_blocking_pids()`).
*   **Опознание владельца**: pid из `application_name` (`app-dataplatform-etl-worker_<pid>`)
    сопоставляется с `task_instance.pid`, поэтому против сессии в отчёте стоит `dag_id.task_id`.
*   **Параметры**: пороги `idle_tx_sec` / `long_query_sec` / `lock_wait_sec`, `alert`, `save_s3`,
    `keep_days`, `dry_run`; хранятся в Variable `tools_pg_activity_cfg`, галка «Сохранить настройки»
    перезаписывает её значениями формы.
*   **Убийство сессий**: параметр `terminate` (по умолчанию выключен и в Variable **не сохраняется**),
    при `dry_run=True` только показывает кандидатов.
*   **Снимки**: при находках пишутся в бакет логов, папка `pg_activity/<дата>/<время>.json`;
    старше `keep_days` даг удаляет сам в таске `prune`. Lifecycle-правило не подошло:
    шлюз отвечает на `PutBucketLifecycleConfiguration` требованием заголовка `Content-MD5`,
    которого boto3 больше не шлёт.
*   **Отчётность**: сводка в заметку, при `alert=True` таск краснеет и уходит уведомление.

### [test_dags.py](test_dags.py)
**Контроль сериализации DAG'ов (`tools_test_dags`).**

Ищет DAG'и, у которых сериализация переписывается на каждом разборе файла, и выясняет
причину. Две независимые группы: `check_serialized` ловит дрожание на парсинге (может
ждать часами), `compare` ведёт версии в S3 и показывает, что изменилось. Отдельно
`parse_time` меряет время разбора файлов и отмечает те, что перевалили половину
`dag_file_processor_timeout`: такой файл обработчик бросит на полпути, и DAG'и из него
исчезнут из метабазы.
*   **Никогда не падает**: одного замера мало, чтобы отличить дрожание от выкладки —
    итог в сводке, а не в статусе задачи.

### [test_kafka.py](test_kafka.py)
**Ручные проверки Kafka (`tools_test_kafka_snd` / `tools_test_kafka_rcv`).**

Два DAG'а для изолированной проверки связки: отправка одного сообщения в топик и просмотр
сообщений из топика.
*   ⚠️ Отправка идёт **мимо очереди** `export.er_sent_files`: файл не попадает в счётчики
    лимитов маршрута и не занимает пул отправителя. Для разовой проверки годится, для
    регулярной отправки нужен тракт, а не этот DAG.

### [make_vault.py](make_vault.py)
**Эмуляция `/vault/secrets/application` для стенда. Не DAG — консольный скрипт.**

Собирает файл, который на боевом контуре кладёт vault, а на стенде класть некому. Формат
повторяет боевой payload sigma DEV, чтобы secret backend ходил по тем же веткам.
*   🔑 Секретов в файле нет: логины и пароли берутся из переменных окружения, умолчания —
    заведомо нерабочие заглушки. Airflow разбирает файл как обычный модуль, ничего не
    выполняя: вся работа под `if __name__ == "__main__"`.

## Сохраняемые параметры

`db_cleanup`, `log_cleanup` и `pg_activity` берут значения по умолчанию из своей Airflow
Variable, а при её отсутствии — из кода. Variable записывается только запуском с галочкой
сохранения и только при изменившихся значениях: разовый эксперимент в UI ночное окно не
двигает, а осознанная правка двигает — без выкладки.

`schedule` — такой же сохраняемый параметр: сам запуск идёт по старому расписанию, новое
подхватывается со следующего разбора файла. Негодное значение таск не записывает (падает),
а уже записанное битым игнорируется в пользу кода — иначе разбор файла упал бы и убрал из
UI саму форму, через которую это чинят.

Убийство сессий в `pg_activity` в Variable **не сохраняется** никогда: следующий запуск по
расписанию никого не убьёт.

## Индикаторы статусов

| Иконка | Статус | Описание |
|---|---|---|
| ✅ | Success | Проверка пройдена успешно |
| ❌ | Fail | Ошибка подключения или логики (AirflowFailException) |
| ☮️ | Skip | Проверка пропущена (например, нет нужной библиотеки) |
| ⏳ | Wait | Ожидание таймаута или события |

---

## Как запустить тесты

1.  Перейдите в Airflow UI.
2.  Найдите DAG `test_hrp_operators` (для операторов) или `test_connections` (для коннектов).
3.  Нажмите **Trigger DAG w/ config**.
4.  Для операторов: укажите целевые `conn_id` и бакет для тестов.
5.  Нажмите **Trigger**.

## Добавление новых тестов

При создании нового оператора в `hrp_operators`:
1.  Зарегистрируйте оператор в `sber_app_dataplatform_etl_core.hrp_operators.__init__`.
2.  Добавьте импорт и таск в соответствующую `task_group` внутри `test_hrp_operators.py`.
3.  Добавьте логику валидации данных после выполнения оператора.
4.  Убедитесь, что `cleanup` корректно удаляет все созданные артефакты.
