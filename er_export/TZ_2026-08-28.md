# 📋 ТЗ: пауза отправки в ТФС, пересоздание `er_wf_meta`, общие настройки, инструкция
*2026-08-28 11:40 MSK · v1.0 · Чуркин Николай · [nschurkin@sber.ru](mailto:nschurkin@sber.ru)*

Задание собрано по переписке 27–28.08.2026: пауза отправки и retry — запрос Григория
Середнева; ключ управляющей таблицы, перенос настроек в переменную и инструкция для
новичка — решения нашей стороны. Ниже — задание целиком, в том виде, в каком оно
согласовано; отметки о ходе работ ведём в git-истории, а не здесь.


Три независимые правки одного фреймворка:

* **A** — пауза отправки в ТФС и её пульт (`plugins/tfs_utils.py`, `tfs_kafka_snd.py`,
  новый `tfs_kafka_setup.py`, `er_export.py`);
* **B** — пересоздание `export.er_wf_meta` с ключом `(replica, extract_name)`, перенос
  `schedule`/`is_recent` в `params`, обязательная строка-дефолт группы (DDL, `er_setup.py`,
  `er_config.py`, `er_export.py`, плюс правка `extract_history`);
* **C** — общие настройки фреймворка переезжают в Variable и правятся из `er_setup`
  первой группой формы (`er_config.py`, `er_setup.py`);
* **D** — инструкция для новичка с готовыми шаблонами (`er_export/QUICKSTART.md`).

B и C трогают одни файлы и делаются одним заходом, причём B — единственная часть
с миграцией данных и окном переключения. A от них независима и выкладывается отдельно.
D пишется **после** B: шаблоны должны учить новой структуре, а не той, что мы отменяем.

---

# Часть A. Пауза отправки в ТФС

## Контекст

Григорий Середнев просит уметь **приостанавливать отправку** очереди — на технические
работы, выкладку доработок, сбой на стороне ТФС. Выгрузка в S3 и постановка в очередь
должны при этом идти по расписанию (выгрузка длинная и дорогая), очередь — копиться,
а после снятия паузы уехать сама. Уровни: конкретная поставка, группа/реплика, маршрут.

Там же два его вопроса:

* **Подтверждение.** Сейчас `_er_wait_confirm` ждёт квитанции по своим `RqUID` до
  `confirm_timeout` (групповой параметр, по умолчанию 60 мин, правится и в форме запуска;
  сверху ограничен `execution_timeout` = таймаут + 5 мин) и падает по истечении, различая
  «очередь стоит» и «отправлено, ответа нет». Любой `StatusCode != 0` роняет таск сразу.
  Таймаут настраиваемый — это уже так.
* **Retry.** Забранный ТФС файл переотправить нельзя, оставшийся в бакете — можно.
  Сейчас `enqueue_files` не проверяет наличие объекта вовсе: опечатка в имени уезжает
  в ТФС и возвращается квитанцией с кодом 101/108. Проверка бакета и есть тот самый
  различитель «можно retry / нельзя».

Сегодня выключателей два, и оба грубые: пауза дага `tfs_kafka_snd` (встаёт весь тракт,
все маршруты) и удаление пула `tfs_<ScenarioId>` (маршрут выпадает из реестра, работает
только на разобранной очереди, сценарии из `TFS_ROUTES` так не выключить).

Выбранные решения: правила паузы — маршрут + реплика + пакет; `wait_confirm` на паузе
**скипается**; состояние — в Variable `tfs_snd_pause`; досылка получает проверку бакета.

## A1. `plugins/tfs_utils.py` (v1.13 → v1.14) — состояние паузы

```python
PAUSE_VAR = 'tfs_snd_pause'
```

Формат — три раздела, в каждом `{ключ: правило}`; правило либо строка-причина,
либо объект `{"until": ISO, "reason": …, "by": …}`:

```json
{
  "scenarios": {"HRPLATFORM-4000": {"until": "2026-09-01T22:00:00+03:00", "reason": "техработы ТФС"}},
  "replicas":  {"hrplatform_datalab__1": "внедрение доработок"},
  "packages":  {"hrplatform_datalab__1__20260828120000": "разбираемся с отказом"}
}
```

Новые функции:

* `pause_rules() -> dict` — читает Variable, нормализует строку/объект к одному виду,
  выбрасывает истёкшие по `until` и неразобранные правила с предупреждением в лог.
  **Ничего не бросает** — как `saved_params` в `plugins/utils.py`: битая переменная
  означает «паузы нет», а не остановку тракта.
* `package_key(row) -> str` — `{replica}__{14 цифр package_ts}`. Ровно тот ключ, что уже
  строит `_package_key` в `tfs_kafka_snd`; функция переезжает сюда, и один формат служит
  и ключом XCom, и ключом паузы.
* `pause_reason(row, rules=None) -> str` — пусто, если строке можно ехать; иначе готовый
  текст. Порядок от частного к общему (пакет → реплика → маршрут), чтобы в сообщении
  стояло самое конкретное правило.
* `split_pending(rows, rules=None) -> tuple[list, list]` — `(свободные, придержанные)`;
  отправитель и проверка затора обязаны делить очередь одинаково.
* `stale_sent(minutes) -> list[dict]` — отправленные больше `minutes` назад, у которых нет
  квитанции. Читает `queue/sent/{сегодня,вчера}/…` (те же две папки, что `_s3_sent_counts`)
  и проверяет наличие `receipts/{rq_uid}.json`. Нужна именно из-за скипа `wait_confirm`:
  пока пакет на паузе, его квитанции не ждёт никто.
* `pause_set(scope, key, until='', reason='', by='')` и `pause_clear(scope, key)` —
  правка переменной для дага-пульта (A6). Возвращают **`(status, message)`** со статусом
  `ok` / `skip` / `fail`, как `store_params` в `plugins/utils.py`: решение о скипе или
  падении принимает таск, там же, где стоят `trigger_rule` и текст для UI. Правило
  добавляется и снимается точечно, остальные разделы переменной не трогаются.
* `pause_summary(rows=None) -> dict` — что сейчас на паузе и что из очереди этим
  придержано: правила по разделам плюс счётчики придержанных строк.

`get_config()` отдаёт `PAUSE_VAR` и `TFS_STALE_MIN`.

## A2. `plugins/tfs_utils.py` — проверка бакета при досылке

В `TFS_ROUTES` у маршрута появляются `conn`, `bucket`, `prefix` (`s3-tfs-hrplt` /
`tfshrplt` / `from/KAP802/hrpl_lm_er` для `HRPLATFORM-4000` — те же значения, что
`S3_CONN`/`BUCKET`/`TFS_MAP` в `er_config.py`; копия осознанная, фабрика ЕР намеренно
не импортирует `tfs_utils` на уровне модуля).

`enqueue_files(...)` возвращает **`(поставленные, пропущенные)`** и перед постановкой
проверяет `check_for_key` в бакете маршрута. Пропущенные — с причиной: «нет в
`s3://tfshrplt/from/…/имя` — либо ТФС уже забрал файл, либо опечатка». У маршрута без
описанного бакета проверки нет (предупреждение в лог), досылка идёт как раньше.
Call site ровно один — `_enqueue_params_once` в `tfs_kafka_snd`.

## A3. `tfs_kafka/tfs_kafka_snd.py` (v2.6 → v2.7)

* `poke_queue`: после отбора своих строк — `free, held = split_pending(queued)`; едет
  только `free`. Придержанное **не попадает в `left`**, по которому считается затор:
  иначе осознанная пауза каждый час роняла бы таск по `TFS_QUEUE_ALERT_MIN`. Это ключевая
  правка — без неё пауза выглядит как авария.
* Заметка о паузе — один раз за окно (флаг в `run_state_*`, как `PARAMS_KEY`), а не каждые
  15 секунд; в `return_value` добавляется `held` — придержанное видно в UI без логов.
* `_finish`: в текст затора добавляется, сколько строк придержано и по какому правилу.
* `poke_scan`: разбирает `(rows, missing)` от `enqueue_files` и показывает пропущенные
  отдельной строкой заметки; раз в час (последний опрос окна) зовёт `stale_sent` и
  предупреждает о неподтверждённых.
* Реестр сценариев пауза не трогает: `drop_unpooled_scenarios` смотрит на пул, пул на месте.

## A4. `er_export/er_export.py` (v3.15 → v3.16)

Проверок паузы ровно две, и обе **вне** цикла ожидания:

* **На входе** — `_pre_await(gcfg, context)` (сейчас `_pre_await(context)`, call site один)
  получает четвёртую причину скипа: пакет уже на паузе, ждать нечего, час занятого слота
  в `datalab_export_er` ни к чему. Строка для `pause_reason` собирается из
  `gcfg['scenario']`, `gcfg['replica']` и метки `_pkg_ts(context)`.
* **На выходе по таймауту** — вместо нынешнего безусловного падения: если правило паузы
  совпало, таск уходит в ☮️ с текстом «квитанций нет, пакет придержан паузой: …»;
  не совпало — падает как раньше, с разбором «очередь стоит / ответа нет».

Внутри цикла паузу **не опрашиваем** намеренно. Пауза, поставленная и снятая за тот же
час, пакета не касается: файлы уедут в том же окне, квитанции придут, цикл закончится
зелёным — а проверка в цикле погасила бы такой пакет на ровном месте. Побочно это
экономит поход в метабазу каждые 10 секунд на каждом ждущем пакете
(см. `project_pg_session_timeouts`): чтений становится два на таск вместо сотен.

Следствие скипа, которое надо назвать вслух в доке: `save_status` отработает
с `confirmed = null`, дельта сдвинется, `schedule_next` продолжит цикл — красных ранов
пауза не даёт, но и подтверждения по этим файлам никто не ждёт. Дыру закрывает
`stale_sent` из A3.

## A5. Новый даг-пульт `tfs_kafka/tfs_kafka_setup.py` (v1.0)

Пульт паузы по образцу `er_export/er_setup.py`: форма запуска → показ состояния →
правка → показ результата. Руками редактировать JSON переменной в UI не придётся,
а неверный ключ или битый `until` не доедет до отправителя.

`schedule=None` (только ручной запуск), `max_active_runs=1`, пул `default_pool` —
пульт не должен занимать слоты тракта.

**Форма:**

| Параметр | Что задаёт |
| :--- | :--- |
| `action` | `👁️ показать` *(по умолчанию)* / `⏸️ поставить на паузу` / `▶️ снять с паузы` / `🧹 убрать истёкшие` |
| `scope` | `scenario` (маршрут) / `replica` (группа) / `package` (один пакет) |
| `key` | значение ключа: `HRPLATFORM-4000`, `hrplatform_datalab__1`, `hrplatform_datalab__1__20260828120000` |
| `until` | до какого времени, ISO; пусто — бессрочно |
| `reason` | зачем — попадает в правило и в заметку |

`key` — свободная строка, а не `enum`: реплики и пакеты живут в очереди, а её читать
на разборе DAG-файла нельзя (это поход в S3). Откуда копировать значение, показывает
таск `show`.

**Таски:**

* `show` — текущие правила (`pause_summary`) плюс сводка очереди: сколько строк ждёт,
  сколько из них придержано и каким правилом, какие сценарии/реплики/пакеты в очереди
  вообще есть. Работает и сам по себе — это и есть `action = показать`.
* `apply` — зовёт `pause_set` / `pause_clear` / чистку истёкших и разбирает статус явно,
  как таск `params` в `check/db_cleanup.py`:
  `skip` (`action = показать`, правило уже такое, снимать нечего) → `AirflowSkipException`,
  `fail` (пустой ключ, `package`-ключ не того формата, `until` не разобрался) →
  `AirflowFailException`.
* `after` — состояние после правки, `trigger_rule=NONE_FAILED` (обязательно: `apply`
  штатно скипает себя, а дефолтный `ALL_SUCCESS` утянул бы в скип и показ результата —
  на этом уже спотыкались в `db_cleanup`).

## A6. Документация

* `tfs_kafka/README.md` — раздел «⏸️ Пауза отправки»: формат переменной, три уровня,
  пульт `tfs_kafka_setup`, чем это отличается от паузы дага и от удаления пула, и что
  постановку в очередь пауза не блокирует.
* `er_export/README.md` — в «📨 Квитанции ТФС» про скип на паузе и движение дельты,
  в «🚚 Отправка в ТФС» про досылку с проверкой бакета.

## Чего не делаем (A)

* Пауза на отдельную таблицу пакета: тикет перечисляет все архивы пакета, задержать часть
  значит отправить ЕР заведомо неполный пакет.
* Блокировку постановки в очередь — заказчик просил обратного.
* Авто-retry по кодам отказа: досылка остаётся ручной, через форму `tfs_kafka_snd`.
* Правку очереди из пульта (удалить строку, переставить порядок) — пульт управляет
  только паузой.

## Проверка (A)

1. **AST-харнессы в scratchpad** (Airflow локально нет): `check_pause.py` — нормализация
   правил (строка / объект / истёкший `until` / битый JSON / незнакомый раздел), приоритет
   пакет → реплика → маршрут, `split_pending`, статусы `pause_set` / `pause_clear`
   (записали / нечего менять / битый ключ / битый `until`); `check_enqueue_check.py` —
   досылка с заглушкой `S3Hook`: файл есть, файла нет, маршрут без бакета.
2. `python3 -m py_compile`; `ruff check plugins/ tfs_kafka/ er_export/` со сверкой с базой.
3. **На стенде sigma** (там `export_er` и `tfs_kafka`): пультом поставить паузу на реплику →
   `pack_zip` и `make_summary` проходят, очередь растёт, `snd_HRPLATFORM-4000` пишет
   «придержано N» и **не краснеет** дольше `TFS_QUEUE_ALERT_MIN`; `wait_confirm` уходит
   в ☮️, `save_status` пишет `confirmed = null`, `schedule_next` работает. Отдельным
   прогоном — пауза, поставленная и снятая ПОКА пакет ждёт: `wait_confirm` обязан
   дождаться квитанций и позеленеть, а не скипнуться. Снять паузу
   пультом → файлы уезжают в порядке `package_ts`, тикет пакета последним. Отдельно
   на пульте: показ без правки, снятие несуществующего правила (☮️), битый ключ пакета
   и `until` в прошлом (❌ и 🧹 соответственно). Досылка: несуществующее имя → отказ
   без RqUID, реальное → отправка и квитанция.
4. Выкладка **одним заходом**: `tfs_utils.py` + `tfs_kafka_snd.py` + `tfs_kafka_setup.py`
   + `er_export.py` (на стенде — `CI06932748/tools/tfs_utils.py` и
   `CI06932748/analytics/datalab/export_er/`), иначе `cannot import name 'pause_reason'`
   и встаёт весь тракт.

## Риски (A)

* **Забытая пауза** — очередь растёт молча, алерт затора по ней намеренно выключен.
  Поэтому `until` в правиле, сводка придержанного в заметке каждого рана и предупреждение
  в логе на каждом опросе.
* **Длинная пауза упирается в суточный лимит**: маршрут отдаёт 2000 файлов в сутки,
  500 в час — накопленное за сутки простоя за один час не уедет. Сказать в README.
* Пакеты, уехавшие «вслепую» (скипнутый `wait_confirm`), видны только через `stale_sent`.

---

# Часть B. Пересоздание `er_wf_meta`: ключ `(replica, extract_name)`

## Контекст

Три вещи, растущие из одного корня:

1. **`db_name = replica` у строки-дефолта** — не смысл, а костыль под
   `ORDER BY (db_name, extract_name)`: `extract_name` у дефолтов пуст, и с пустым
   `db_name` дефолты всех групп схлопнулись бы в одну строку. Признаком группы должен
   быть **пустой `extract_name`**, и больше ничего.
2. **Одна таблица в двух группах невозможна** — ключ `(db_name, extract_name)` не даёт
   держать `evolution.lc_items_opened` в двух репликах, то есть нельзя завести новую
   версию группы, не убив старую.
3. **Расписание живёт в трёх местах**: `build_wfs` берёт cron из строки-дефолта, а если
   там пусто — из **первой попавшейся** строки-поставки (`er_setup.py:407-418`). Какая
   победит, зависит от того, у кого поле заполнено.

Совместимость не нужна — таблица **пересоздаётся** с ключом `(replica, extract_name)`,
заодно перетряхивается структура: `replica` первой колонкой, `schedule` и `is_recent`
переезжают в `params`, строка-дефолт группы становится обязательной.

## B1. `er_export/er_wf_meta.sql` (v1.2 → v2.0) — новая таблица

```
replica, extract_name, schema_name, db_name,
pk, uk, fields, sql_from, sql_where, sql_join, sql_with, sql_settings,
params, description, is_active, updated_at
ENGINE = ReplicatedReplacingMergeTree('/clickhouse/tables/er_wf_meta_{uuid}', '{replica}', updated_at)
ORDER BY (replica, extract_name)
```

* `replica` первой — она же первая часть ключа и первое, что смотрят глазами.
  Дальше `extract_name`, затем `schema_name` (целевая схема в `.meta`) и только потом
  `db_name` (база источника в CH): сначала то, чем поставка называется на стороне ЕР,
  потом откуда она читается.
* `db_name` — обычная колонка: у строки-дефолта пустая, требование «равен replica»
  снимается вместе с проверками в `build_wfs` и в таске `check`.
* Колонки `schedule` и `is_recent` уходят — см. B3.
* Комментарии в DDL переписываются под новые правила (сейчас там ровно то, что мы
  отменяем: `db_name = replica`, cron из поставки, «is_recent не наследуется»).

## B2. Ключ записи в коде

* `raw_key(row)` → `f"{replica}/{extract_name}"`, у дефолта — `f"{replica} (дефолты группы)"`;
  `key_to_where(key)` разбирает обратно в `(replica, extract_name)`. Разделитель `/`,
  а не `.`: точка занята видом `db.table` и на новом ключе читалась бы неоднозначно.
* `_group_row(hook, replica)` — прямой `WHERE replica = … AND extract_name = ''`
  вместо чтения всех дефолтов и сравнения `replica_full` в питоне.
* `COLUMNS` в `er_setup` повторяет порядок DDL (это и порядок `INSERT`, и порядок вывода
  записи в лог и заметку): `replica`, `extract_name`, `schema_name`, `db_name`, …;
  `schedule` и `is_recent` убраны. `merge_patch`
  на них отвечает не «неизвестное поле», а подсказкой «переехало в `params`».
* В Variable `datalab_er_wfs` ключ внутри `group['tables']` — **`extract_name`**
  (в пакете он теперь уникален по построению), а `db_name` кладётся **в саму запись**:
  фабрика берёт базу оттуда, а не режет ключ по точке (`_table_cfg` в `er_export.py`).
* `tg` (id TaskGroup, префикс XCom, имя параметра `tbl_<tg>`) остаётся прежним
  `f"{db}__{extract_name}"` — менять его значит переименовать таски и потерять историю
  ранов ради косметики.

## B3. `schedule` и `is_recent` — в `params`

* `schedule` — ключ `params` строки-дефолта группы, и только там (см. B4).
  `explicit_schedule(row)` начинает читать разобранные `params`.
* `is_recent` — ключ `params` поставки. Побочно чинится давняя оговорка: колонка
  `UInt8 DEFAULT 0` не позволяла отличить «не задано» от «явно delta», поэтому
  `is_recent` не наследовался вовсе. В JSON ключ либо есть, либо нет — наследование
  от группы работает само, как у остальных параметров, и запись про исключение уходит
  из `INHERITED` и из DDL.
* Больше в `params` не переносим: `schema_name` наследуется и участвует в `.meta`,
  `description` собирается по своему правилу (своё → комментарий CH → групповое),
  `is_active` должен фильтроваться в `WHERE`, а не разбором JSON.
* Оба ключа добавляются в `GROUP_PARAMS` / `TABLE_PARAMS` соответственно — тогда их
  подхватывает и слияние, и проверка «групповой ключ в строке-поставке».

## B4. Строка-дефолт группы обязательна

* Расписание рабочего пакета — **только** из строки-дефолта. Фолбэк на поставку убирается.
* Нет строки-дефолта → ошибка группы с готовым рецептом:
  `{"replica": "<группа>", "params": "{\"schedule\": \"30 2 * * *\"}"}`. Группа уходит
  в Variable заглушкой, как любая другая ошибка настройки.
* **Расписание заглушки** фолбэк сохраняет: если группового cron нет, `create_broken_dag`
  получает первый непустой из поставок — иначе сломанная группа перестала бы краснеть
  в своём ритме, ради чего заглушка и заведена.
* Cron проверяется `valid_schedule` (`CronTriggerTimetable(...).validate()`, та же
  проверка, что у tools-чистильщиков). Сейчас битый cron доезжает до
  `DAG(schedule_interval=…)` и роняет разбор файла — то есть **все** пакеты ЕР разом.
* `schedule` в `params` поставки — предупреждение «игнорируется, расписание берётся
  только из строки-дефолта». Групповые ключи в `params` поставки — предупреждение
  «задавайте их в строке-дефолте»; поставку не выбрасываем.
* В таске `check`: поставка без строки-дефолта своей группы — ошибка с тем же рецептом
  (сейчас проходит молча и ломается только на синке); строка-дефолт без `schedule` —
  ошибка вместо нынешнего предупреждения (`er_setup.py:718`), отдушина — галка «Записать
  несмотря на проверку».

## B5. Состояние дельты тоже ключуется репликой

Прямое следствие возможности «одна таблица в двух группах», и без него новая свобода
молча портит данные: `export.extract_history` и `export.extract_current_vw` ключуются
**только** `extract_name` (`sql_cur_delta` в `er_export.py:110`, `INSERT` в `save_status`).
Две группы с одной таблицей делили бы одно состояние: вторая увидела бы окно первой
и выгрузила пустоту, а её `save_status` переписал бы чужую историю.

* В `extract_history` добавляется `replica`, во вью `extract_current_vw` — она же
  и в группировку/окно, по которому выбирается текущее состояние.
* `sql_cur_delta(tbl)` → `sql_cur_delta(replica, tbl)`; `_er_save_status` пишет `replica`.
* DDL этих объектов в репозитории **нет** — снять `SHOW CREATE` со стенда и завести
  `er_export/er_extract_history.sql`, раз уж всё равно пересоздаём.

## B6. `er_export/er_config.py` (v1.13 → v1.14)

`valid_schedule` добавляется в блок импорта-реэкспорта из `plugins.utils` /
`CI06932748.tools.utils` — рядом с `add_note`, `ensure_pool`, `get_dict_from_ch`,
`on_callback`.

⚠️ Зависимость выкладки: `valid_schedule` появилась в `plugins/utils.py` v1.6, и на стенде
она живёт в `CI06932748/tools/utils.py`, который **ещё не выложен** (висящая выкладка
tools). Часть B выкладывается только вместе с ним, иначе `cannot import name`.

## B7. Документация

* `er_export/README.md`: «Два типа строк» (признак группы — пустой `extract_name`,
  строка-дефолт обязательна), «Колонки» (новый состав и ключ), «➕ Как завести новый
  пакет» (сначала строка-дефолт с `schedule` в `params`), новый абзац про две группы
  с одинаковыми таблицами и про то, что состояние дельты у них разное.
* `doc_md` у `er_setup.py`: примеры под новую структуру.

## B8. Миграция и переключение

1. Создать `export.er_wf_meta_v2` по новой DDL (`ON CLUSTER`).
2. Перелить с трансформацией: `SELECT … FROM export.er_wf_meta FINAL`, где `schedule`
   и `is_recent` вливаются в `params`. В ClickHouse это `jsonMergePatch` — сначала
   проверить, что функция есть на кластере; если нет, перелить одноразовым питоновским
   скриптом (собрать JSON в питоне надёжнее, чем конкатенацией строк в SQL).
3. Сверить построчно: число строк, число групп, что у каждой группы есть дефолт и в его
   `params` лежит cron.
4. Поставить `export_er_setup` **на паузу**, чтобы синк не сработал в момент подмены.
5. `RENAME TABLE export.er_wf_meta TO export.er_wf_meta_old,
   export.er_wf_meta_v2 TO export.er_wf_meta ON CLUSTER` — одним запросом. В БД Atomic
   UUID таблицы переименование переживает, поэтому ZK-путь на `{uuid}` остаётся валидным.
6. Выложить код (`er_config.py`, `er_setup.py`, `er_export.py`, DDL-файлы), снять
   `export_er_setup` с паузы, прогнать **с галкой «Синхронизировать принудительно»**.
7. `er_wf_meta_old` не удалять до подтверждения: откат — обратный `RENAME` плюс откат кода.

Пакеты ЕР на время шагов 4–6 держать на паузе: они читают Variable, а та в этот момент
пересобирается под новую структуру.

## Проверка (B)

1. AST-харнесс `check_build_wfs.py` в scratchpad (функция чистая, заглушки не нужны):
   группа с cron в `params`; группа без cron; группы нет вовсе (ошибка + расписание
   заглушки взято из поставки); битый cron; `schedule` и групповые ключи в `params`
   поставки (предупреждения, поставка на месте); **две группы с одной и той же таблицей** —
   обе собираются, ключи `tables` не пересекаются.
2. `python3 -m py_compile`, `ruff check er_export/`.
3. На стенде, после переключения: пакеты собрались, расписания взяты групповые, `recent`-
   поставка осталась `recent`; завести вторую группу с той же таблицей и убедиться, что
   у неё **своё** состояние дельты (`extract_current_vw` отдаёт по паре replica+таблица),
   а `save_status` пишет две отдельные строки; специально испорченная группа (убрать cron)
   стала заглушкой с внятной причиной.

## Чего не делаем (B)

* Не переносим в `params` идентификацию и SQL: `replica`, `extract_name`, `db_name`,
  `schema_name`, `sql_*`, `pk`, `uk`, `fields`, `is_active` остаются колонками.
* Не переименовываем TaskGroup и таски пакетов — история ранов дороже единообразия.
* Не переносим групповые параметры из поставок автоматически: где они заданы, скажет
  предупреждение синка, переносит человек.

---

# Часть C. Общие настройки фреймворка — в Variable, правка из `er_setup`

## Контекст

Константы `er_config.py` (маршруты реплик, лимиты стендов, умолчания параметров, коннекты
и бакет) сейчас правятся только выкладкой. Переносим их в Variable, а форму `er_setup`
делим на две группы: **первая — общие настройки, вторая — запись `er_wf_meta`, как сейчас**.
Механика та же, что у tools-чистильщиков (`project_tools_saved_params`): код задаёт
запасной вариант, переменная — рабочий.

## C1. `er_export/er_config.py` (v1.13 → v1.14) — чтение переопределений

```python
CFG_VAR_NAME = 'datalab_er_config'
```

* `cfg_overrides() -> dict` — читает Variable через try/except (как `saved_params`
  и `known_scenarios`): недоступная или битая переменная означает «переопределений нет»
  и предупреждение в лог. Ронять разбор нельзя — это **все** пакеты ЕР разом.
* Переопределяемые ключи — явный белый список с типом из кода:

  | Ключ | Зачем |
  | :--- | :--- |
  | `TFS_MAP` | маршрут новой реплики без выкладки |
  | `LIMITS` | лимит строк по стендам |
  | `DEFAULT_PARAMS` | умолчания групповых и табличных параметров (слияние **по ключам** через `merge_params`, только известные имена) |
  | `CH_ID`, `S3_CONN`, `BUCKET` | переезд контура без выкладки |

  Всё остальное остаётся в коде: `DEF_ARGS` содержит функции; `FORMAT_MAP`, `TYPE_MAP`,
  `EXTRA_PRE/SUF`, `HIVE_RESERVED` — это формат обмена с КАП, а не настройка;
  `TS_POOL_SLOTS` обязан быть 1 по устройству пула меток времени; `POOL_SLOTS` не берём,
  потому что `ensure_pool` существующий пул не трогает — правка выглядела бы применённой,
  не будучи ею.
* Значение `null` в переменной = «вернуть значение из кода».
* Неизвестный ключ или несовпадение типа — предупреждение и игнор (а не падение);
  зато в форме `er_setup` тот же случай — ошибка, потому что там за экраном человек.

`get_config()` уже отдаёт снимок констант, а `wf_checksum` уже включает его в сумму —
значит правка общих настроек **сама** вызывает пересборку пакетов на следующем синке,
и специальных костылей не нужно. Ценой одного лишнего чтения Variable на разбор файла
(по разу в `er_export.py` и в `er_setup.py`) — тот же порядок, что у `known_scenarios`.

## C2. `er_export/er_setup.py` — форма из двух групп и таск правки

Поля формы группируются `Param(..., section=…)`: **⚙️ Общие настройки** (первой) и
**📋 Запись er_wf_meta** (существующие `record`, `patch`, `force_write`, `force_sync`).
В первой — одно поле `config_patch`: JSON только с изменяемыми ключами, `null` возвращает
значение из кода. Порядок секций в UI задаётся порядком первого появления в `params`;
если на стенде окажется иначе — оставить вторую группу без секции (поля без секции
Airflow рисует сверху) и поменять их местами.

Новый таск `config`:

* пустой патч → `AirflowSkipException` («только просмотр»), но текущее состояние всё равно
  печатается: значение из кода, переопределение, итог — по каждому ключу белого списка;
* неизвестный ключ, неверный тип, `TFS_MAP` не парой `(scenario_id, prefix)`, нецелой
  `LIMITS` → `AirflowFailException` со списком причин, переменная не трогается;
* запись — `Variable.set(..., serialize_json=True, description=f"{ts} MSK · {run_id}")`,
  как в `store_params`; итог в `add_note` и в XCom.

Место в цепочке — параллельная ветка, сливающаяся на синке:

```python
[config(), apply(check(prepare()))] >> sync()
```

Ветка именно параллельная: поставь `config()` перед `prepare()`, и штатный скип правки
конфига утянул бы в скип весь разбор записи. `sync` уже `trigger_rule='none_failed'`,
так что переживает скип обеих веток.

⚠️ **Применяется со следующего разбора файла.** Константы читаются на парсинге, поэтому
синк в этом же ране работает ещё со старым снимком и, скорее всего, уйдёт в скип по сумме.
Через ~30 секунд шедулер перечитает файл, снимок изменится, сумма разойдётся — и
следующий синк соберёт пакеты уже по новым настройкам. Это должно быть написано и в
заметке таска, и в `doc_md`, иначе выглядит как «правка не применилась».

## C3. Документация

* `doc_md` у `er_setup.py` уже печатает снимок конфига — рядом дописать, какие ключи
  переопределяемы и что сейчас переопределено.
* `er_export/README.md` — раздел «⚙️ Общие настройки»: где лежат, что можно менять,
  как вернуть значение из кода, когда правка вступает в силу.

## Проверка (C)

1. Харнесс `check_er_cfg.py` в scratchpad: слияние (пусто / известный ключ / `null` /
   неизвестный ключ / неверный тип), глубокое слияние `DEFAULT_PARAMS`, битый JSON
   переменной → значения из кода плюс предупреждение.
2. На стенде: поменять `LIMITS` и одно умолчание из `DEFAULT_PARAMS` → убедиться, что
   `doc_md` и следующий синк их подхватили, а пакеты пересобрались; вернуть `null`-ом
   и проверить откат к коду. Отдельно — заведомо неверный тип: ран красный, переменная цела.

## Чего не делаем (C)

* Не выносим в переменную формат обмена (`FORMAT_MAP`, `TYPE_MAP`, служебные колонки) —
  это контракт с КАП, его правка должна проходить через ревью и выкладку.
* Не делаем «применить немедленно»: перечитывание констант внутри рана потребовало бы
  пересборки DAG-ов в том же процессе, чего Airflow не умеет.

---

# Часть D. `er_export/QUICKSTART.md` — инструкция для новичка

## Контекст

`er_export/README.md` — справочник на 769 строк: он отвечает на вопрос «как это устроено»,
но не на «мне надо завести выгрузку, что нажимать». Человеку со стороны нужен короткий
путь от нуля до едущего пакета, с текстами, которые можно скопировать в форму.

Пишется **после части B** и по её структуре: шаблон, учащий класть `schedule` в колонку,
через неделю станет ловушкой.

## Что внутри

1. **Три понятия за пять строк** — реплика/группа = пакет = один DAG = один тикет,
   поставка = одна таблица, строка-дефолт группы. Дальше — только «что делать».
2. **Пять шагов**, каждый с готовым JSON-патчем для формы `export_er_setup`:
   строка-дефолт группы → первая поставка → просмотр и проверка (пустой патч) →
   синхронизация → снять пакет с паузы и запустить руками.
3. **Что смотреть в запущенном пакете**: `make_ts` → `export_to_s3` → `pack_zip` →
   `make_summary` → `wait_confirm`, где лежат файлы, где очередь и квитанции, где
   состояние дельты. По абзацу на таск, без внутренностей.
4. **Готовые шаблоны** (копировать и править): простая таблица; таблица с JOIN и алиасами;
   `recent`-поставка; `full_export` без поля времени; выгрузка в JSON. Плюс один пример
   `INSERT` для тех, кто правит через clickhouse-client, а не через форму.
5. **Частые ошибки** таблицей «симптом → причина → что делать»: пустой или звёздочный
   `fields`; квалификатор таблицы в имени колонки при двух и более JOIN; нет строки-дефолта
   группы; cron не разобран; пакет не подтверждается (очередь/пауза/квитанция с кодом);
   дельта не двигается (`ad_hoc`, снятый флаг таблицы); «правка не применилась»
   (константы читаются на разборе файла, см. часть C).
6. **Шпаргалка по параметрам** — только те, что реально трогают: `schedule`, `increment`,
   `time_field`, `overlap`, `format`, `full_export`, `send_empty`, `auto_confirm`,
   `notify_kafka`, `confirm_timeout`. С умолчаниями и одной строкой смысла. Полный список
   остаётся в README, ссылкой.
7. Если часть A уже выложена — абзац «как приостановить отправку»: пультом
   `tfs_kafka_setup`, что при этом происходит с пакетом и с дельтой.

## Связки

* Шапка `er_export/README.md` — строка «Первый раз здесь? → `QUICKSTART.md`»;
  сам README остаётся справочником и не дублирует шаги.
* `doc_md` у `export_er_setup` — та же отсылка текстом (в UI ссылка на файл репозитория
  не кликается, поэтому именно путь, а не ссылка).

## Проверка (D)

Пройти собственную инструкцию на стенде **буквально**, копируя патчи из файла: завести
тестовую группу и одну поставку, довести до зелёного рана и до квитанции. Всё, где
пришлось догадываться или лезть в README, — правится в инструкции.
