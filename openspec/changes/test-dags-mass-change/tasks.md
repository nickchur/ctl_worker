# Tasks

- [x] `find_changed` возвращает счётчики, список — только в XCom `changed_dags` — `check/test_dags.py`
- [x] `snapshot_dags` отбирает изменившиеся сам, пары на сравнение режет по `COMPARE_LIMIT`,
      остаток — в `not_compared` и `snapshot_stats` — `check/test_dags.py`
- [x] Строка в сводке про несравнённые — `check/test_dags.py`
- [x] Замер и причина лимита — комментарием у `COMPARE_LIMIT`; `check/readme.md`
- [x] Проверка на стенде: изменившихся больше лимита — прогон зелёный, версии записаны всем,
      сравнены первые, повторный прогон изменений не находит. 12.09.2026, лимит занижен до 5,
      20 подсаженных версий: изменилось 19, записано 19, сравнено 5, `not_compared` 14, прогон
      success; второй прогон — изменений 0
- [ ] При архивации — строка в «Происхождение» `openspec/specs/check/spec.md`: потолок
      сравнений из-за лимита XCom-бэкенда core (500 элементов); пересматривать, если лимит
      снимут или сравнение перестанет быть mapped-таском
