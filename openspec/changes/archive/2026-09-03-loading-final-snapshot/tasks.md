# Tasks

## 1. Общий способ класть снимок

- [x] 1.1 `ctl_loading_snapshot(lid, wf_name, key, ld=None)` в `plugins/ctl_core.py`:
      готовый объект или `GET /v4/api/loading/{lid}`, нормализация `ctl_loading_norm`,
      запись `ctl_obj_save`; проверить обе ветки (с объектом и без)
- [x] 1.2 `ctl_chk_status` переходит на него для `ctl_working/{lid}`; проверить, что
      содержимое ключа не изменилось

## 2. Итог в run_end

- [x] 2.1 `_finalize_status` возвращает ответ финализирующего вызова пятым элементом;
      проверить обе ветки — завершение и TIME-WAIT
- [x] 2.2 Номер попытки берётся до вызова `_finalize_status` (внутри мутируется тот же
      словарь `wfp_retry`); проверить, что первая попытка даёт `_0`
- [x] 2.3 Снимок `ctl_done/{lid}_{try}` кладётся сразу после финализации и до веток
      `raise`; проверить на успехе, на повторе и на ошибке
- [x] 2.4 Ошибка сохранения не роняет таск: предупреждение в лог и заметку; проверить с
      недоступным бакетом

## 3. Документация

- [x] 3.1 `ctl_worker/readme.md`: раскладка бакета — `ctl_working/`, `ctl_loadings/`,
      `ctl_done/`; строки версии в тронутых файлах
- [x] 3.2 `ruff check .`, `openspec validate --all --strict`, `check_context.py`

## 4. Проверка на стенде

- [x] 4.1 Полный цикл на эмуляторе: успех → `ctl_done/{lid}_0.json` с финальным статусом,
      `ctl_working/{lid}.json` остался состоянием на вход
- [x] 4.2 Повтор: `_0` с ожиданием повтора, `_1` с итогом, файлы не затирают друг друга
- [x] 4.3 Сборщик фикстур даёт тот же набор загрузок, что и до правки
