# Tasks

## 1. Чтение ответа из лога

- [x] 1.1 `plugins/ctl_utils.py`: функция, читающая `vw_swf_ctl_log` по `loading_id` и
      возвращающая `end_msg` последней завершённой попытки либо `None`. Таймаут короткий:
      это справка, а не работа. Проверка: на фикстуре лога запрос возвращает JSON ответа,
      на пустой таблице — `None`.
- [x] 1.2 `run_exe`: на повторной попытке сначала спросить лог. Ответ найден — запушить как
      `result` и выйти; не найден или Greenplum не ответил — упасть с внятным текстом, не
      трогая `pr_swf_start_ctl`. Проверка: подменённый лог даёт успешный таск без обращения
      к процедуре.
- [x] 1.3 Разрешить `run_exe` одну повторную попытку и переписать комментарий у неё: сейчас
      он говорит «ретрай = повторный ETL», а после правки повтор ETL не запускает.
      Проверка: у таска `retries=1`, в комментарии названо, зачем именно попытка нужна.

## 2. Пустой таск не занимает канал к Greenplum

- [x] 2.1 Снять `pool='gp_pool'` с `run_out`: тело пустое, в Greenplum он не ходит, а слотов
      в пуле намеренно меньше, чем разрешено в самом Greenplum. Проверка: в определении
      таска пула нет, публикация `DatasetAlias` работает по-прежнему.

## 3. Проверка

- [x] 3.1 Поднять на стенде копию `tb_swf_ctl_log` и `vw_swf_ctl_log` из `GP/srv_wf`,
      наполнить фикстурой (завершённая загрузка, незавершённая, отсутствующая) и прогнать
      на ней запрос из 1.1. Проверка: три случая дают ответ, `None` и `None`.
- [x] 3.2 `ruff check .`, `openspec validate --all --strict`, `check_context.py`.
- [x] 3.3 Выяснено на живом Greenplum: **обрыв клиента запрос не останавливает** — ETL
      доработает и закоммитится сам. Отсюда третий исход в 1.2: «ответа нет, но работа
      идёт». Проверка: подтверждено на боевом контуре.
- [ ] 3.4 Там же: оборвать `run_exe` в середине и убедиться, что повторная попытка
      дожидается ответа. Проверка: таск зелёный, в журнале Greenplum одна запись `end`, а
      не две. Стенд не годится — Greenplum там нет.
