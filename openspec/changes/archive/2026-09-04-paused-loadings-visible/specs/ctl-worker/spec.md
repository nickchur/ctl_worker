## ADDED Requirements

### Requirement: Запаузенный даг не запускается молча

Если даг воркфлоу стоит на паузе, сенсор MUST NOT запускать его: запуск паузу не
проверяет и создаёт ран, который не поедет, пока паузу не снимут. Сенсор SHALL пропускать
загрузку, называя происхождение паузы. Загрузка в CTL MUST NOT изменяться: снимут паузу —
она поедет сама.

Происхождение SHALL называться честно. Если в журнале метабазы есть событие постановки
паузы — SHALL называться его автор и время. Если паузу поставил рубильник тракта (ключ
конфигурации, выключающий профиль целиком) — SHALL называться он. В остальных случаях
запись отсутствует, и происхождение MUST NOT угадываться: пауза с создания дага и пауза,
поставленная кодом или через REST, по метабазе неразличимы, и ответ SHALL называть оба
варианта. Уверенно названная не та причина хуже отсутствия ответа: оператор снимет паузу,
поверив, что за ней ничего не стоит.

Сенсор MUST NOT снимать паузу самостоятельно: у дага воркфлоу, не стоящего на расписании
в CTL, снятие паузы включает и его собственное расписание Airflow, о котором CTL не просил.

Пропуск SHALL оставлять след, пригодный для счёта: «сколько раз за сутки» должно
считаться запросом, а не чтением логов. След SHALL быть разрежен по времени — сенсор
опрашивает CTL раз в минуту, а состояние паузы так часто не меняется. Неудача записи следа
MUST NOT менять исход: пропуск остаётся пропуском.

#### Scenario: Даг на паузе, CTL просит запуск

- **WHEN** загрузка активна, а даг её воркфлоу на паузе
- **THEN** запуск не делается, загрузка пропускается с указанием происхождения паузы, а
  её статус в CTL остаётся прежним

#### Scenario: Паузу поставил рубильник тракта

- **WHEN** даг на паузе, записи в журнале нет, а в конфигурации стоит ключ массовой паузы
- **THEN** причиной называется рубильник, а не свойства воркфлоу

#### Scenario: Записи о паузе нет

- **WHEN** даг на паузе, а в журнале метабазы событий о ней нет
- **THEN** ответ называет оба возможных происхождения и не выдаёт одно из них за
  установленное

### Requirement: Загрузки, ждущие снятия паузы, видны в мониторе

Монитор SHALL показывать отдельным списком рядом со сводкой SLA те загрузки, которым есть
куда двигаться прямо сейчас, а даг стоит на паузе — с именем воркфлоу и возрастом
ожидания, считая его так же, как для SLA: от последней смены статуса загрузки.

Загрузки, которым пауза не мешает, MUST NOT попадать в список: выполняющаяся, доигрывающая
и ожидающая своего срока паузы не ждут, и заметка о них воспитывает привычку её не читать.
Даг, выпавший из воркфлоу, в список также MUST NOT попадать: его загрузке нужна
диагностика пропавшего дага, а не ожидание.

Список SHALL умещаться в заметку: при большом числе ожидающих SHALL показываться их число
и старейшие, а полный список SHALL оставаться в логе таска. Заметка ограничена по длине, и
длинный список вытесняет из неё сводки, записанные тем же проходом.

#### Scenario: Загрузка ждёт четвёртый час

- **WHEN** загрузка активна, её даг на паузе, и с последней смены статуса прошло четыре часа
- **THEN** в сводке монитора видно имя воркфлоу и это время, без чтения логов сенсора

#### Scenario: Загрузка выполняется, а даг на паузе

- **WHEN** воркер загрузки работает, а даг её воркфлоу поставили на паузу
- **THEN** в списке ожидающих её нет: пауза остановит следующий запуск, а не этот

#### Scenario: Ожидающих больше, чем влезает в заметку

- **WHEN** снятия паузы ждут десятки загрузок
- **THEN** в заметке видно их число и старейшие, а полный список — в логе таска

## Происхождение

| Требование | Откуда шрам | Когда пересматривать |
| :--- | :--- | :--- |
| Запаузенный даг не запускается молча | `trigger_dag` в Airflow не проверяет паузу: создаёт ран сразу в `queued`, а планировщик запаузенный даг не разбирает — ран висит вечно. Проверено на Airflow 2.10.1 | Когда запуск научится отказывать на паузе (или отдавать это решению вызывающего) — проверка в сенсоре станет лишней |
| Происхождение паузы называется неточно, когда записи нет | Метабаза не хранит, кто и когда поставил паузу, если её ставили не через UI или CLI: наш `update_dag_pause` и REST API пишут прямо в `DagModel`, следа в `log` не оставляя | Появится в Airflow отдельный след постановки паузы — «одно из двух» можно будет заменить точным ответом |
