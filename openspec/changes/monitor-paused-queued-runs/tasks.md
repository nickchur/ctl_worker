# Tasks

## 1. Отбор санитара

- [x] 1.1 Третий случай — ран в очереди у запаузенного дага
      *Проверка:* режим показа перечисляет такие раны отдельной строкой
- [x] 1.2 Разделение исходов: «ехать некуда» закрывает загрузку, «ждёт паузы» — нет
      *Проверка:* боевой прогон на стенде, загрузка остаётся `ACTIVE`
- [x] 1.3 `up_for_reschedule` в списке живых состояний задачи
      *Проверка:* ран ждущего `tfs_wait` старше порога не попадает в отбор
- [x] 1.4 Потолок на заход и полное число находок в режиме показа
      *Проверка:* заметка показывает «найдено N, потолок 500»
- [x] 1.5 Нижняя граница порога `zombie_after`
      *Проверка:* при `seconds=60` таск падает с объяснением

## 2. Документы

- [x] 2.1 Дельта спецификации `ctl-worker`
      *Проверка:* `openspec validate --all --strict`
- [x] 2.2 Раздел в `ctl_worker/readme.md`, строки версии
      *Проверка:* `python3 .claude/scripts/check_context.py`
- [x] 2.3 `ruff check .` без новых замечаний
