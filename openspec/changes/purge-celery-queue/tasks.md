# Задачи

## 1. DAG `tools_queue_cleanup`

- [x] 1.1 `check/queue_cleanup.py`: таск `collect` — обход очередей и приоритетных ключей,
      сверка `llen` с числом прочитанных, разбор тела сообщения (base64 → argv), разметка по
      состоянию задачи, названной в команде
- [x] 1.2 Дамп полных тел мусорных сообщений в S3 внутри `collect`: payload'ы не должны
      ехать через XCom, а удалять нужно ровно те тела, что размечены
- [x] 1.3 Таск `purge` — удаление по `LREM` с дописанным префиксом, только при `purge=True`,
      с порогами доли и абсолютного числа
- [x] 1.4 Таск `report` — сводка: было / мусора / удалено / осталось, расхождения с
      `external_executor_id`, адрес дампа
- [x] 1.5 Таск `prune` — дампы старше `keep_days`
- [x] 1.6 Параметры из Variable по образцу `pg_activity`; `purge` в переменную не сохраняется

## 2. Проверка на стенде

- [x] 2.1 Живой брокер стенда (`aftest-redis`), очередь `qc_probe` из 10 сообщений на
      неподвижных задачах: разметка дала 4 мусора / 6 живых / 1 нечитаемую цель /
      1 расхождение — ровно как ожидалось
- [x] 2.2 Удаление: 4 удалено, 0 не найдено, 6 осталось. Возврат из дампа через `rpush`
      восстановил все 10, включая сообщение на приоритетном ключе
- [x] 2.3 Задача в незавершённом состоянии остаётся живой, в терминальном — мусор.
      На первом прогоне состояния на стенде поплыли между выборкой и запуском, и числа
      это отразили — набор пересобран на неподвижных задачах
- [x] 2.4 `global_keyprefix = {qctest}`: даг видит префикс и читает все 10 сообщений, а не 0;
      удаление по префиксованным ключам верное
- [x] 2.5 Сторож ключей: копия дага с убранным `prefix +` остановилась на
      `llen=5, прочитано 8` и не удалила ничего
- [x] 2.6 Пороги: доля 0.40 при `min_junk_share=0.5` и мусор 4 при `max_delete=2` — оба
      падают с объяснением, ничего не удалив
- [x] 2.7 Стенд убран: тестовые ключи redis удалены, запись дага-копии из метабазы убрана

## 3. Документация

- [x] 3.1 `check/readme.md` — раздел про инструмент и порядок работы
- [x] 3.2 Строки версии подняты в изменённых файлах

## 4. Осталось сделать руками

- [ ] 4.1 Выложить `check/queue_cleanup.py` на alpha и снять с паузы
- [ ] 4.2 Прогон подсчёта на боевой очереди: сверить долю мусора с ожидаемой (~0.97)
- [ ] 4.3 Прогон с `purge=True` после сверки дампа
