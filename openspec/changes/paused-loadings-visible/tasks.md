# Tasks

## 1. Сенсор

- [x] 1.1 Проверка паузы перед запуском, пропуск загрузки без изменения её статуса
      *Проверка:* загрузка на запаузенный даг пропускается, статус в CTL прежний
- [x] 1.2 Происхождение паузы: человек, рубильник тракта, «записи нет»
      *Проверка:* четыре ветки на стенде, включая «сняли, потом поставили без записи»
- [x] 1.3 Событие в журнал метабазы — не чаще раза в час на даг, в try/except
      *Проверка:* вторая запись подряд не пишется; при сбое базы таск не падает
- [x] 1.4 Время в причине — в часовом поясе контура
      *Проверка:* в ответе MSK, а не UTC

## 2. Монитор

- [x] 2.1 Список «ждут снятия паузы»: только те, кому есть куда ехать
      *Проверка:* шесть случаев на стенде — выполняющаяся, доигрывающая и спящая не
      попадают, не стартовавшая и требующие перезапуска попадают
- [x] 2.2 Фильтр `is_active`: выпавший из воркфлоу даг в список не идёт
- [x] 2.3 Заметка не переполняется: счёт, десяток старейших, полный список в логе

## 3. Документы

- [x] 3.1 Дельта спецификации `ctl-worker` с разделом «Происхождение»
      *Проверка:* `openspec validate --all --strict`
- [x] 3.2 `ctl_worker/readme.md`: происхождения паузы, оба рубильника, критерий списка
      *Проверка:* `python3 .claude/scripts/check_context.py`
- [x] 3.3 Строки версий; `ruff check .` без новых замечаний
