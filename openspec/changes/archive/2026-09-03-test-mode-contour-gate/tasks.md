# Tasks

## 1. Общий хелпер контура

- [x] 1.1 `env_stand()` в `plugins/utils.py`: `ENV_STAND`, при отсутствии `ENVIRONMENT`,
      верхний регистр, пусто при отсутствии обеих — проверить `python -c` с подменой
      окружения на всех трёх случаях

## 2. Симулятор

- [x] 2.1 `ctl_worker/ctl_test.py`: гейт регистрации по контуру (DEV/IFT/PSI), вне списка —
      предупреждение в лог и выход без создания DAG'а; проверить `airflow dags list` под
      `ENV_STAND=PROM` и под `ENV_STAND=DEV`
- [x] 2.2 Режим из ключа `simulator` (`off`/`event`/`dataset`/`trigger`), `off` — пропуск
      с заметкой; проверить `airflow tasks test … test_events` при `off` и при `dataset`
- [x] 2.3 Событийный режим только на DEV: вне DEV — пропуск с заметкой о причине;
      проверить под `ENV_STAND=IFT` с `simulator: event`

## 3. Тестовый режим воркера

- [x] 3.1 `ctl_worker/ctl_worker.py`: разбор `test_mode` в профиль (`off`/`ok`/`ok-no`/
      `ok-no-error`, устаревшие истинные значения → `ok-no` с предупреждением) и гейт по
      контуру (DEV/IFT) с заметкой при игнорировании; проверить `run_exe` под `PSI`
- [x] 3.2 Убрать `wf_test_mode` из источников режима; проверить на эмуляторе воркфлоу с
      этим параметром — выполняется по-настоящему
- [x] 3.3 Коды исхода по профилю вместо `random.randint(0,2)`, подстановка видна в заметке
      `Result`; проверить многократным прогоном под `ok` и под `ok-no-error`
- [x] 3.4 Верхняя граница `pg_sleep` из ключа `test_sleep`; проверить, что при
      `test_sleep: minutes=1` таск укладывается в минуту

## 4. Конфигурация и документация

- [x] 4.1 `ctl_worker/ctl_config.py`: умолчания `simulator`, `test_mode`, `test_sleep` и
      `Param(enum=…)` для первых двух; проверить, что форма запуска показывает списки, а
      первый запуск на пустой Variable по-прежнему её создаёт
- [x] 4.2 `ctl_config.json`, `ctl_worker/readme.md`, `ctl_worker/testbed/README.md`:
      матрица контуров, новые ключи, снятие предупреждения «выключайте `test_mode`»;
      проверить `.claude/scripts/check_context.py`
- [x] 4.3 Строка версии во всех тронутых модулях, пересборка `CONTEXT.md`

## 5. Проверка на стенде

- [x] 5.1 Прогон под четырьмя значениями `ENV_STAND` (DEV, IFT, PSI, PROM): наличие дага
      симулятора, поведение событийного режима и тестового режима — по матрице из спеки
- [x] 5.2 Полный цикл воркфлоу на эмуляторе под `test_mode: ok-no-error`: встречаются
      успех, «нет данных» и ошибка с уходом в TIME-WAIT, `wf_exe` не правится
- [x] 5.3 `ruff check .` и `openspec validate --all --strict`
