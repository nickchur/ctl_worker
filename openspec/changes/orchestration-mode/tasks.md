# Tasks

## 1. Режим в конфигурации

- [x] 1.1 Ключи `orchestrator` (`ctl`/`mixed`/`af`) и `pause_new_dags` в `ctl_config.py`:
      умолчания, списки значений в форме; проверить, что без ключей поведение прежнее
- [x] 1.2 Чтение режима в фабрике одним местом; проверить разбор под каждым значением

## 2. Фабрика

- [x] 2.1 `get_schedule` учитывает режим: `ctl` — только датасет ТФС, `mixed` — как сейчас,
      `af` — расписание всегда; проверить на стенде по одному воркфлоу каждого вида
- [x] 2.2 `is_paused_upon_creation` берётся из `pause_new_dags`; проверить на новом воркфлоу
- [x] 2.3 Предупреждение о двойном запуске в режиме `af`; проверить, что разбор не падает

## 3. Проверка и документация

- [x] 3.1 Переключение режимов на стенде: `parse_time` и `serialized_gap` после каждого
- [x] 3.2 Ручной запуск из CTL проходит в каждом режиме (эмулятор)
- [x] 3.3 `ctl_worker/readme.md`, `openspec/project.md`, строки версий; `ruff`, `openspec validate`
