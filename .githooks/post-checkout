#!/usr/bin/env bash
# Переключились на другую ветку — в ней могут быть другие спеки, навыки и команды.
# $3 = 1 при смене ветки, 0 при checkout отдельного файла: во втором случае молчим.
[ "$3" = "1" ] || exit 0
exec "$(git rev-parse --show-toplevel)/.githooks/_sync" "после checkout"
